name: Create Gitea Release Pre-Existing Tag
on:
  workflow_call:
    inputs:
      tag:
        type: string
        required: true
      body:
        type: string
        required: false
    secrets:
      ACTIONS_TOKEN:
        required: true

jobs:
  create_release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Create Release
        uses: https://git.tainton.uk/actions/create-release-action@v1.1.4
        with:
          release_name: ${{ inputs.tag }}
          tag: ${{ inputs.tag }}
          body: ${{ inputs.body }}
          token: ${{ secrets.ACTIONS_TOKEN }}
